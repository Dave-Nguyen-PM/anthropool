---
description: Add or refresh the currently logged-in Claude Code account in anthropool
argument-hint: "[--slot N]"
allowed-tools: Bash(anthropool add:*)
---

# /anthropool:add

Registers the currently logged-in Claude Code account with anthropool, or
refreshes its saved credentials if it is already managed.

```bash
anthropool add $ARGUMENTS
```
