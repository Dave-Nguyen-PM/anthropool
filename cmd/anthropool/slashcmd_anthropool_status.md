---
description: Show the active Claude Code login and anthropool state
allowed-tools: Bash(anthropool status)
---

# /anthropool:status

Shows the current live Claude Code login and anthropool account state.

```bash
anthropool status
```
